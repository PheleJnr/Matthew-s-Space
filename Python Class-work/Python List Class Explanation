
//Append Examples: 

Append takes in one argument at a time, you don't need to specify the index

scores = [45,89,12,72,55]

scores.append(99)



//Insert: 

Insert specify the position before the element. if you specify a number outside the bound of the list, it automatically insert it at the end of the list 

scores = [45,89,12,72,55]

scores.insert(1, 88)


//Concatenation of Two List with the + sign: A new sequence is created

scores = [45,89,12,72,55]

amounts = [12,9,8,4,3,1,0]

number = scores + amounts


//Extend():

Extend takes in a collection of its own and put the value in the variable you called it on  

extend can take in only one int but a collection of integers 

scores = [45,89,12,72,55]

scores.extend([2,5,1,0,6])


//Remove: you must specify the element you want to remove unless it will throw error

when you have same numbers, it removes the first occurence 

scores = [45,89,12,72,55]

scores.remove(72)


//Pop: If you did not specify the index will remove the number at the last index.

whenever it pops out an element, it returns the element.

scores = [45,89,12,72,55]

scores.pop()

ole.append(scores.pop())



//del Statement: unties the value from the reference. meaning the values cannot be referenced again 



//Chnaging Items: List are mutable 



//Unpacking Sequences: The element in a sequenced collection like list can be unpacked by assigning the sequence to a comma-seperated list of variables.

A ValueError occurs if the number of variables to the left of the assignment symbol is not identical to the number of elements in the sequence on the right 

scores = [4,8,3,1,0]

number1, number2, number3, number4, number5 = scores
print(number1, number2, number3, number4, number5, sep="\n")

unpack doesnt change the original values from the scores variable, however if you want to unpack only two it will throw an error of ValueError of too many values to unpack if you don't specify where to put the remaining values in the list.

how to resolve that, we would need to specify where the rest will be:

number1, number2, *others = scores  //use (wildcard *) to collect the remaining items into a list

when unpacking small many elemnts into too many variables: not enough values to unpack. The number of variables must match the number of items 

a,b,c,d = [1,2,3] 


//Random Module:

The random module in python is a built in library used for generating pseudo-random numbers and performing random based operations.

Pseudo-random numbers are numbers that can be randomly predicted. The computer system doesnt know how to perform at random.

Examples:

random.randit(a, b): 

random.randrange(start, stop, step):
 
random.random(): it is used to generate random floating numbers 

random.choice: It takes in a collection. 

random.shuffle: 

random.seed: 

random.choices: same thing with random.choice but the diff is you can specify more than one name


// Random.ranint


import random

for _ in range _(10):

    number = random.randint(1,7)

    print(number, end=" ")

print


//Random.seed()

import random


random.seed(8)

for _ in range _(10):

    number = random.randint(1,7)

    print(number, end=" ")

print































































































